# {{ ansible_managed }}

filter {

  grok {
    match => { "message" => "%{SYSLOGBASE} %{GREEDYDATA:syslog_message}" }
  }

  date {
    timezone => "UTC"
    match  => [ "timestamp", "MMM dd YYY HH:mm:ss", "MMM  d YYY HH:mm:ss", "ISO8601", "MMM dd HH:mm:ss" ]
  }

}
